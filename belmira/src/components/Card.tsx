import React from 'react';
import { Box } from '@mui/material';

function Card() {
  return (
    <Box
      sx={{
        backgroundColor: '#b71540',
        borderRadius: '16px',
        width: '200px',
        height: '200px',
      }}
    >
    </Box>
  );
}

export default Card;